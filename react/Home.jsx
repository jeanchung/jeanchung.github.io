import React, { PropTypes } from 'react';

const Home = props => (
  <div className="container">
    <div className="page-header">
      <h2 className="page-title">Hello, World!</h2>
    </div>
    <p>Welcome to my React app starter.</p>
  </div>
);


export default Home;
